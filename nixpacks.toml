[phases.setup]
nixPkgs = []
aptPkgs = ["python3", "python3-pip"]

[phases.install]
cmds = ["pip3 install --break-system-packages -r requirements.txt"]

[start]
cmd = "python3 -m uvicorn api:app --host 0.0.0.0 --port ${PORT:-8200}"
